{% extends 'base.html.twig' %}

{% block body %}
    <h1>Articles de la catégorie {{ category.name }}</h1>
    {% for article in articles %}
        <h2>Article n°{{ article.id }} : {{ article.title}}</h2>
        <p>{{ article.content }}</p>
    {% endfor %}

{% endblock %}